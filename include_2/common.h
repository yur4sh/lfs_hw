#ifndef COMMON_H
#define COMMON_H

#define ASCII_SHIFT 32
#define IS_UPPER(c) (c >= 'A' && c <= 'Z')
#define IS_LOWER(c) (c >= 'a' && c <= 'z')

#include <string.h>
#include <stdlib.h>
#include <stdio.h>

#endif // COMMON_H